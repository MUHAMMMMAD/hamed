# متطلبات مصغّرة للاستضافة المشتركة (cPanel) ذات الموارد المحدودة.
# تشغّل النواة الكاملة: التسعير، المخاطر، القرار، التدفقات، المجلس،
# لوحة الويب، تقارير Excel، واستيراد BOQ من Excel/CSV.
#
# الميزات الثقيلة (قراءة PDF/DXF، والانحدار الخطي في التعلّم) تتدرّج بأمان:
#   - PDF/DXF: تظهر رسالة واضحة لتثبيت المكتبة عند الحاجة.
#   - التعلّم: يستخدم المتوسط التاريخي بدل الانحدار عند غياب numpy.
#
# لتفعيلها لاحقاً أضف: pymupdf  ezdxf  numpy

pydantic>=2.0
fastapi>=0.110
uvicorn>=0.27
python-multipart>=0.0.9
httpx>=0.27
openpyxl>=3.1
python-dotenv>=1.0
requests>=2.31
a2wsgi>=1.10          # جسر ASGI->WSGI لتشغيل FastAPI تحت Passenger/cPanel
